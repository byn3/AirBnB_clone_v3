Get a State by it's ID
---
tags:
  - All States
produces:
  - application/json
  - application/xml
parameters:
  - in: path
    # name: state_id
    # description: ID of the State to get
    # type: string
    # required: true
responses:
  200:
    description: OK
    schema:
      type: string
