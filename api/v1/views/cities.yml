Get a City by it's ID
---
tags:
  - City
produces:
  - application/json
  - application/xml
parameters:
  - in: path
    name: city_id
    description: ID of the City to get
    type: string
    required: true
responses:
  200:
    description: OK
    schema:
      type: string
